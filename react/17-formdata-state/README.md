# 📝 useState: как сохранить данные формы в состояние

## ✅ Ответ

```js
const [formData, setFormData] = useState({ login: '', password: '' });

const handleChange = (e) => {
  setFormData({
    ...formData,
    [e.target.name]: e.target.value,
  });
};

return (
  <form>
    <input
      name="login"
      value={formData.login}
      onChange={handleChange}
      placeholder="Логин"
    />
    <input
      name="password"
      type="password"
      value={formData.password}
      onChange={handleChange}
      placeholder="Пароль"
    />
  </form>
);
```

---

## 💡 Что важно помнить

- `useState` создаёт **иммутабельное состояние**, поэтому при изменении формы нужно **создавать новый объект**, а не мутировать старый.
- Прямое изменение (например, `formData.login = 'Alex'`) не вызовет ререндер, потому что React не «увидит» новое состояние.
- Для нескольких полей удобно использовать один обработчик с `[e.target.name]`, как в примере выше.
- Можно упростить обработку, если использовать `useReducer` — особенно при сложных формах с валидацией или несколькими шагами.

---

## ⚠️ Что могут спросить дополнительно

> «Почему нельзя менять state напрямую?»

Потому что React сравнивает **старую и новую ссылку** на объект.  
Если вы мутируете объект, ссылка не меняется, и React не понимает, что нужно перерендерить компонент.

---

## 🧩 Совет

Если форма большая, вынеси состояние и логику в **кастомный хук** `useForm` или используй готовые решения — например, **React Hook Form** или **Formik**. Они решают задачи валидации, сабмита и оптимизации.