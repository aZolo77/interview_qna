# 🪟 Подписка на изменение размера окна и очистка при размонтировании

## ✅ Пример реализации

```js
const WindowSizeTracker = () => {
  const [width, setWidth] = useState(window.innerWidth);

  useEffect(() => {
    const handleResize = () => {
      setWidth(window.innerWidth);
    };

    window.addEventListener('resize', handleResize);

    // Очистка подписки при размонтировании
    return () => {
      window.removeEventListener('resize', handleResize);
    };
  }, []); // Пустой массив = эффект выполнится один раз при монтировании

  return <div>Ширина окна: {width}px</div>;
};
```

---

## 💡 Объяснение

- `useEffect` с **пустым массивом зависимостей (`[]`)** запускается **только один раз** — при монтировании компонента.
- Возвращаемая из эффекта функция выполняется при **размонтировании** — это и есть **очистка побочных эффектов**.
- Без очистки обработчик `resize` остался бы висеть в памяти, даже если компонент удалён → **утечка памяти** и лишние вызовы.

---

## ⚙️ Альтернатива: Кастомный хук

Можно вынести логику в хук, чтобы переиспользовать:

```js
const useWindowWidth = () => {
  const [width, setWidth] = useState(window.innerWidth);

  useEffect(() => {
    const handleResize = () => setWidth(window.innerWidth);
    window.addEventListener('resize', handleResize);
    return () => window.removeEventListener('resize', handleResize);
  }, []);

  return width;
};

const Example = () => {
  const width = useWindowWidth();
  return <p>Текущая ширина окна: {width}px</p>;
};
```

---

## 🧠 Что могут спросить дополнительно

> «Почему нельзя вызвать `setWidth(window.innerWidth)` вне useEffect?»

Потому что в момент первого рендера React ещё может не отрисовать всё дерево — при SSR (Server Side Rendering) `window` вообще не существует, и код упадёт с ошибкой. Поэтому такие обращения нужно **делать внутри эффектов**, которые выполняются **только на клиенте**.