# ⚙️ Как организовать работу с API в React

---

## 🧩 Подходы к работе с API

1. **Fetch API**
   - Встроенный метод браузера.
   - Требует ручной обработки ошибок, преобразования JSON, отмены запросов и таймаутов.
   - Подходит для простых кейсов, но быстро становится громоздким.

2. **Axios**
   - Библиотека с более удобным синтаксисом и встроенной обработкой JSON.
   - Поддерживает:
     - **интерсепторы** (например, для токенов авторизации);
     - **отмену запросов**;
     - **настройку базового URL**;
     - **таймауты и обработку ошибок**.

3. **React Query (TanStack Query)**
   - Продвинутая работа с асинхронными данными:
     - кэширование;
     - рефетчинг при фокусе окна;
     - инвалидация данных;
     - автоматическая синхронизация с сервером;
     - поддержка мутаций (POST/PUT/DELETE).
   - Отлично подходит для **SPA с частыми API-запросами**.

---

## 💻 Пример с React Query и Axios

```js
import { useQuery } from '@tanstack/react-query';
import axios from 'axios';

const fetchUsers = async () => {
  const res = await axios.get('/api/users');
  return res.data;
};

const Users = () => {
  const { data, isLoading, error } = useQuery(['users'], fetchUsers);

  if (isLoading) return <p>Loading...</p>;
  if (error) return <p>Error: {error.message}</p>;

  return (
    <ul>
      {data.map((user) => (
        <li key={user.id}>{user.name}</li>
      ))}
    </ul>
  );
};
```
---

## 🧠 Best Practices

- Используй **React Query** для продвинутого кеширования и оптимизации запросов.
- Для глобальной обработки токенов и ошибок добавь **Axios interceptors**.
- Вынеси логику API в отдельный слой (например, `/services/api.js`), чтобы компоненты оставались чистыми.
- Никогда не храни секретные ключи или токены в коде фронтенда.