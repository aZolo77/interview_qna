# ⚖️ Когда использовать Context API, Redux или Zustand

## 🧩 Context API
**Когда использовать:**  
- Небольшие приложения, где нужно передать данные на несколько уровней вниз (тема, язык, текущий пользователь).  
- Простой state sharing без сложной логики.

**Плюсы:**
- Встроен в React, не требует зависимостей.  
- Простая реализация.  

**Минусы:**
- Каждый контекст вызывает **ререндер всех потребителей**, даже если они не используют изменённые данные.  
- Не подходит для часто изменяющихся состояний (например, ввод текста, счётчики и т.д.).  

**Пример:**

```js
const ThemeContext = createContext();

const App = () => {
  const [theme, setTheme] = useState('light');

  return (
    <ThemeContext.Provider value={{ theme, setTheme }}>
      <Toolbar />
    </ThemeContext.Provider>
  );
};

const Toolbar = () => {
  const { theme, setTheme } = useContext(ThemeContext);
  return (
    <button onClick={() => setTheme(theme === 'light' ? 'dark' : 'light')}>
      Switch theme
    </button>
  );
};
```

---

## 🏗 Redux
**Когда использовать:**  
- Крупные приложения с глобальным состоянием.  
- Нужно **предсказуемое управление состоянием**, **middleware** (thunk, saga), **DevTools** для отладки.  
- Несколько модулей должны синхронно реагировать на изменения.  

**Плюсы:**
- Централизованное хранилище и строгие правила работы с ним.  
- Отлично работает с серверным рендерингом (SSR).  
- Большая экосистема и инструменты разработчика.  

**Минусы:**
- Много boilerplate, особенно без Redux Toolkit.  
- Избыточен для мелких проектов.

**Пример (с Redux Toolkit):**

```js
import { configureStore, createSlice } from '@reduxjs/toolkit';
import { Provider, useDispatch, useSelector } from 'react-redux';

const counterSlice = createSlice({
  name: 'counter',
  initialState: { value: 0 },
  reducers: {
    increment: (state) => { state.value += 1; },
    decrement: (state) => { state.value -= 1; },
  },
});

const store = configureStore({ reducer: { counter: counterSlice.reducer } });

const Counter = () => {
  const dispatch = useDispatch();
  const value = useSelector((s) => s.counter.value);
  return (
    <>
      <button onClick={() => dispatch(counterSlice.actions.decrement())}>-</button>
      <span>{value}</span>
      <button onClick={() => dispatch(counterSlice.actions.increment())}>+</button>
    </>
  );
};
```

---

## 🪶 Zustand
**Когда использовать:**  
- Средние приложения, где Redux кажется избыточным.  
- Нужно **простое, но мощное состояние** с поддержкой селекторов, middleware и сохранением в localStorage.  

**Плюсы:**
- Минимальный код, высокая производительность.  
- Не вызывает массовые ререндеры (в отличие от Context API).  
- Простая интеграция с React без провайдеров.  

**Минусы:**
- Меньшая экосистема, чем у Redux.  
- Нет встроенных DevTools, как у Redux Toolkit (но можно добавить плагином).  

**Пример:**

```js
import { create } from 'zustand';

const useStore = create((set) => ({
  count: 0,
  increment: () => set((state) => ({ count: state.count + 1 })),
}));

const Counter = () => {
  const { count, increment } = useStore();
  return <button onClick={increment}>{count}</button>;
};
```

---

## ⚙️ Итог
| Инструмент | Когда использовать | Плюсы | Минусы |
|-------------|--------------------|--------|---------|
| **Context API** | Простая передача данных между компонентами | Без зависимостей, встроен в React | Ререндер всех потребителей |
| **Redux** | Большие приложения с логикой и отладкой | Предсказуемость, DevTools, экосистема | Много boilerplate |
| **Zustand** | Средние приложения, альтернатива Redux | Лёгкий, производительный | Меньше инструментов и экосистемы |

💡 **Если ты не пользовался Zustand** — это не критично. На собеседовании можешь сказать, что знаешь о нём как о "легковесной альтернативе Redux", и пока предпочитаешь Redux Toolkit за счёт зрелости и инструментов.