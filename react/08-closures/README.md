# 🌀 Как работают замыкания в обработчиках

**Замыкание** — это функция, которая запоминает переменные из внешней области видимости, даже после того, как эта область перестала существовать.

---

## ⚠️ Проблема с замыканиями в React

При каждом рендере создаются **новые функции и новое состояние**, но обработчик может замкнуться на **старое значение state**.

Пример:

```js
const Component = () => {
  const [count, setCount] = useState(0);

  const handleClick = () => {
    // count навсегда "замкнут" в момент первого рендера
    setCount(count + 1);
  };

  return <button onClick={handleClick}>Click</button>;
};
```

Если ререндеры происходят не сразу или есть асинхронность, `count` может быть неактуальным — обновления будут «теряться».

---

## ✅ Решение — функциональное обновление

```js
setCount(prev => prev + 1);
```

Так React получает **самое свежее значение** `state` и обновляет его на основе предыдущего состояния, а не на замкнутом в памяти.

---

## 💡 Итого
- Замыкание "запоминает" старое значение переменной.
- В обработчиках это может привести к багам.
- Используй **функциональные обновления** или **useCallback** с зависимостями.