# ⚙️ Что такое debounce и throttle, и где применяются в React

---

## 🕓 Debounce
**Debounce** откладывает выполнение функции, пока не прекратятся частые вызовы.

**Применение:**  
— поиск по инпуту,  
— автодополнение,  
— валидация при вводе,  
— фильтрация при наборе текста.

**Пример:**

```js
import { useDebouncedCallback } from 'use-debounce';

const Search = () => {
  const [query, setQuery] = useState('');
  
  const debounced = useDebouncedCallback((value) => {
    fetchResults(value);
  }, 500); // функция вызовется через 500мс после последнего ввода

  return <input onChange={(e) => debounced(e.target.value)} />;
};
```

---

## ⏱ Throttle
**Throttle** ограничивает частоту вызова функции — она выполняется **не чаще**, чем раз в заданный интервал.

**Применение:**  
— обработка событий `scroll`, `resize`,  
— трекинг положения мыши,  
— обновление прогресса загрузки.

**Пример с lodash:**

```js
import { throttle } from 'lodash';

const handleScroll = throttle(() => {
  console.log('scroll event');
}, 1000); // не чаще, чем раз в секунду

window.addEventListener('scroll', handleScroll);
```

---

## 💡 Итого
| Механизм  | Когда вызывается | Применение |
|------------|------------------|-------------|
| **Debounce** | После паузы | поиск, инпуты |
| **Throttle** | Через равные интервалы | scroll, resize |

- Debounce — “подожди, пока пользователь закончит”.  
- Throttle — “не чаще, чем раз в X миллисекунд”.