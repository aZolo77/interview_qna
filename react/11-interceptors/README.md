# 🛡 Что такое интерсепторы в контексте Axios

**Интерсепторы (interceptors)** — это механика **перехвата запросов и ответов** в Axios до того, как они будут отправлены или обработаны.  
Они позволяют **централизованно управлять** поведением HTTP-запросов — добавлять заголовки, логировать, обновлять токен и т.д.

---

## ⚙️ Как это работает

Axios позволяет зарегистрировать два типа интерсепторов:

1. **Request Interceptor (до отправки запроса)**  
   Используется для:
   - добавления токена авторизации;
   - логирования запросов;
   - изменения тела запроса.

2. **Response Interceptor (после получения ответа)**  
   Используется для:
   - обработки ошибок (например, 401 Unauthorized);
   - автоматического обновления токена (refresh token);
   - логирования ответов.

---

## 💻 Пример

```js
import axios from 'axios';

const api = axios.create({
  baseURL: '/api',
});

// 🧩 Перехват исходящих запросов
api.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('token');
    if (token) config.headers.Authorization = `Bearer ${token}`;
    return config;
  },
  (error) => Promise.reject(error)
);

// 🧩 Перехват входящих ответов
api.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      // например, разлогинить пользователя
      console.error('Unauthorized — redirect to login');
    }
    return Promise.reject(error);
  }
);

export default api;
```

---

## 🚀 Преимущества

- Централизованная логика — не нужно повторять одни и те же проверки в каждом запросе.
- Проще реализовать **автоматическую авторизацию/разлогинивание**.
- Можно глобально перехватывать и обрабатывать **ошибки сети**.
- Улучшается структура и читаемость кода — компоненты не знают о деталях API.

---

**Итог:**  
Интерсепторы в Axios — это “middleware” между вашим кодом и сервером, которое позволяет гибко управлять каждым запросом и ответом без дублирования логики.