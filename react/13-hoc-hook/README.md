# 🧩 HOC и Custom Hook — в чём разница?

## 🔼 HOC (Higher-Order Component)
**Что это:**  
Функция, которая принимает компонент и возвращает новый компонент с добавленным поведением.  
Использовались до появления хуков для повторного использования логики.

**Пример:**

```js
const withLogger = (Component) => (props) => {
  console.log('Props:', props);
  return <Component {...props} />;
};

// Использование
const UserProfile = (props) => <div>{props.name}</div>;
export default withLogger(UserProfile);
```

**Минусы HOC:**
- Оборачивают компоненты в дополнительные уровни (глубокая вложенность).  
- Могут портить читаемость JSX.  
- Конфликт имён пропсов.  
- Труднее отлаживать и тестировать.

---

## 🪝 Custom Hook
**Что это:**  
Функция, которая использует встроенные хуки React (useState, useEffect, useMemo и т.д.)  
Позволяет переиспользовать логику между компонентами без оборачивания их в HOC.

**Пример:**

```js
const useLogger = (props) => {
  useEffect(() => {
    console.log('Props:', props);
  }, [props]);
};

// Использование
const UserProfile = (props) => {
  useLogger(props);
  return <div>{props.name}</div>;
};
```

**Плюсы Custom Hooks:**
- Нет лишних обёрток в дереве компонентов.  
- Легче читать, тестировать и комбинировать.  
- Современный и рекомендуемый подход (с React 16.8+).  

---

## ⚖️ Сравнение

| Критерий | HOC | Custom Hook |
|-----------|-----|--------------|
| Подход | Оборачивает компонент | Встраивается внутрь компонента |
| Кодовая сложность | Выше | Ниже |
| Тестирование | Сложнее | Проще |
| Производительность | Могут создавать лишние уровни | Лёгкие и чистые |
| Современность | Старый паттерн | Современный стандарт |

---

💡 **Совет:**  
Сегодня **хуки** почти полностью заменили HOC. Если задача — переиспользовать логику, **пиши Custom Hook**.  
HOC стоит использовать только для совместимости с легаси-кодом или сторонними библиотеками.