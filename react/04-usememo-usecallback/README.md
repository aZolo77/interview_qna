# ⚙️ useMemo и useCallback — оптимизация производительности в React

## 🧠 Что делает useMemo
**useMemo** кэширует **результат вычисления** функции между рендерами.  
React будет повторно использовать сохранённое значение, если зависимости не изменились.

### Пример:

```js
const total = useMemo(() => calculateSum(items), [items]);
```

📌 Здесь `calculateSum` выполнится **только при изменении `items`**, а не при каждом рендере.

---

## ⚡ Что делает useCallback
**useCallback** кэширует **саму функцию**, а не результат её выполнения.  
Используется, чтобы дочерние компоненты не перерендеривались из-за новой ссылки на функцию.

### Пример:

```js
const handleClick = useCallback(() => doSomething(id), [id]);
```

📌 Теперь `handleClick` не будет пересоздаваться, если `id` не изменился.  
Это особенно важно при передаче коллбеков в **мемоизированные компоненты** (через `React.memo`).

---

## 🧩 Когда использовать

| Сценарий | Что использовать | Зачем |
|-----------|------------------|--------|
| Дорогостоящие вычисления (например, фильтрация, сортировка) | useMemo | Избежать лишних пересчётов |
| Передача коллбеков в дочерние компоненты | useCallback | Предотвратить ненужные ререндеры |
| Сложные вычисления внутри JSX | useMemo | Сделать код чище и эффективнее |

---

## ⚠️ Когда **не нужно** использовать
- Если вычисления простые и не влияют на производительность — кэш не оправдан.  
- Избыточное использование useMemo/useCallback может **усложнить код** и не дать выигрыша в скорости.

---

## 🧮 Как это работает под капотом
React сохраняет значение (или ссылку на функцию) между рендерами.  
При каждом обновлении он сравнивает зависимости:
- Если зависимости изменились — пересчитает значение или пересоздаст функцию.
- Если нет — вернёт закэшированное.

---

## ✅ Совет
> Используй useMemo и useCallback **только тогда**, когда реально есть проблема с производительностью.  
> Добавление их «на всякий случай» — как прикручивание турбины к самокату 😄