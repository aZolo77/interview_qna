# ⚛️ Чем отличается Class-компонент от Функционального

## 📘 Основная идея
React-компоненты можно писать двумя способами:
1. **Class-компоненты** — старый подход, основанный на ES6 классах.  
2. **Функциональные компоненты** — современный стандарт, основанный на функциях и **хуках**.

---

## 🧩 Сравнение

| 💡 Характеристика | 🏫 Class-компонент | ⚙️ Функциональный компонент |
|--------------------|-------------------|-----------------------------|
| Состояние (state) | Использует `this.state` и `this.setState()` | Использует `useState()` |
| Методы жизненного цикла | `componentDidMount`, `componentDidUpdate`, `componentWillUnmount` | `useEffect()` |
| Контекст `this` | Обязательно (нужно биндить методы или использовать стрелочные функции) | Нет `this`, контекст управляется замыканиями |
| Синтаксис | Более громоздкий (много boilerplate) | Короткий и читаемый |
| Тестирование | Сложнее из-за состояния и методов | Проще, особенно с хуками |
| Производительность | Немного уступает (из-за лишних оберток) | Чище и быстрее |
| Актуальность | Используется в легаси-коде | Рекомендуется с React 16.8+ |

---

## 💻 Пример Class-компонента

```js
class Counter extends React.Component {
  constructor(props) {
    super(props);
    this.state = { count: 0 };
  }

  handleClick = () => {
    this.setState({ count: this.state.count + 1 });
  };

  render() {
    return (
      <div>
        <p>Clicked: {this.state.count}</p>
        <button onClick={this.handleClick}>+</button>
      </div>
    );
  }
}
```

---

## 💻 Пример Функционального компонента

```js
function Counter() {
  const [count, setCount] = React.useState(0);

  return (
    <div>
      <p>Clicked: {count}</p>
      <button onClick={() => setCount(count + 1)}>+</button>
    </div>
  );
}
```

---

## 💬 Ключевые различия
- **Хуки заменили методы жизненного цикла** — вместо `componentDidMount` → `useEffect`.  
- **Нет необходимости использовать `this`**, что устраняет многие баги.  
- **Функциональные компоненты легче тестировать, переиспользовать и понимать**.  
- **React рекомендует хуки** как стандарт для новых приложений.

---

## ✅ Совет
> Всегда выбирайте **функциональные компоненты**, если не работаете с легаси-кодом.  
Они короче, чище и открывают доступ к современным возможностям React.
