# 🚫 Почему нельзя напрямую мутировать `state`

**State** в React должен оставаться **иммутабельным** (неизменяемым напрямую).  
Если изменить его напрямую, React **не заметит изменений** и **не перерендерит компонент**.

---

## ❌ Неправильно

```js
// Классовый компонент
this.state.name = 'Alex'; // Ререндер не произойдёт!

// Функциональный компонент
user.name = 'Alex'; 
setUser(user); // Тоже не сработает!

В этих случаях React не узнаёт, что данные изменились,  
так как объект `user` остался тем же по ссылке.
```

---

## ✅ Правильно

```js
// Классовый компонент
this.setState({ name: 'Alex' });

// Функциональный компонент
const [user, setUser] = useState({ name: 'Bob' });
setUser({ ...user, name: 'Alex' }); // Создаём новый объект
```

---

## 🧠 Почему так
React использует **поверхностное сравнение (shallow compare)**.  
Он проверяет, изменилась ли ссылка на объект.  
Если ссылка осталась прежней — React решает, что ничего не изменилось.

---

## 🧩 Запомни правило
> Не мутируй — **создавай копию**.  
>  
> React не экстрасенс: если ты изменил объект втихую, он этого не узнает.