# 🔼 Поднятие состояния (Lifting State Up)

**Поднятие состояния** — это приём, когда состояние, нужное нескольким компонентам, **переносится в их общего родителя**.  
Так React обеспечивает **единый источник истины (single source of truth)**, а данные передаются сверху вниз через пропсы.

---

## 🧠 Суть идеи
Когда два или больше компонента должны **делить одно состояние** (например, значение инпута, фильтр, выбранный элемент),  
вместо того чтобы дублировать `useState` в каждом из них — **поднимаем состояние выше**.

---

## 💡 Пример

```js
// Родительский компонент
const Parent = () => {
  const [value, setValue] = useState('');

  return (
    <>
      <ChildA value={value} onChange={setValue} />
      <ChildB value={value} />
    </>
  );
};

// Дочерние компоненты
const ChildA = ({ value, onChange }) => (
  <input value={value} onChange={(e) => onChange(e.target.value)} />
);

const ChildB = ({ value }) => <p>{value}</p>;
```

---

## ⚙️ Как это работает

1. `Parent` хранит общее состояние `value`.
2. `ChildA` изменяет это состояние, вызывая `onChange`.
3. `ChildB` автоматически получает обновлённое значение через пропсы.

Результат — синхронизация состояний без путаницы и дублирования логики.

---

## 🚫 Когда **не нужно** поднимать
- Если состояние нужно **только одному компоненту**, оно должно оставаться локальным.  
- Если данных слишком много и приходится «прокидывать пропсы через 3–4 уровня» — стоит рассмотреть **Context API**.

---

## ✅ Совет
> Поднимай состояние до **наименьшего общего предка**, но не выше, чем нужно.  
> Иначе родитель будет перерендериваться слишком часто, как офисный принтер на сдаче отчёта 😅