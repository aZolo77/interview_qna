# 🔑 Что делает `key` в списках React

**`key`** — это специальный проп, который помогает React **отслеживать элементы списка** при их изменении, добавлении или удалении.

---

## 🧠 Зачем нужен `key`
Когда React сравнивает старый и новый Virtual DOM, он использует `key`, чтобы понять:
- какой элемент остался прежним,  
- какой добавился,  
- а какой нужно удалить.

Без `key` React может «перепутать» элементы и **перерисовать весь список**, вместо минимального обновления.

---

## ⚠️ Плохой пример

```js
{items.map((item) => (
  <li>{item.name}</li> // Warning: Each child in a list should have a unique "key" prop
))}
```

React не может отличить элементы, если их порядок или содержимое поменяются.

---

## ✅ Хороший пример

```js
{items.map((item) => (
  <li key={item.id}>{item.name}</li>
))}
```

Теперь у каждого элемента есть уникальный `key`, и React обновит только изменённые строки.

---

## 🧩 Правила выбора ключей
- Используй **уникальный и стабильный идентификатор** (например, `id` из базы).  
- Не применяй индекс массива (`index`) как `key`, **если список изменяется** (удаления, сортировка).  
- Если список статичен и никогда не меняется — индекс можно использовать без проблем.

---

## 🚀 Резюме
`key` нужен не для отображения, а для **эффективного обновления DOM**.  
Без него React слеп, как браузер без вкладок.